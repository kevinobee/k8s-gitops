# Linkerd-Viz Dashboard Extension

To install and open the Linkerd UI run:

```shell

linkerd viz install | kubectl apply -f -
linkerd viz check
linkerd viz dashboard &
```

The Linkerd dashboard will open in a browser.
