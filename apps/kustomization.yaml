apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - linkerd
  - argocd
  # disabled - cert-manager
  - gatekeeper
  - cluster-addons
  - ingress-nginx
  - loki
  - metallb
  - sealed-secrets
